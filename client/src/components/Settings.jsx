import React from 'react';

const Settings = () => {
    return (
        <div className="settings">
            <h2>Settings</h2>
            {/* settings infromation */}
        </div>
    );
};

export default Settings;
